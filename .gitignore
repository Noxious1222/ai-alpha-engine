git add data docs src tests .gitignore git commit -m "chore: scaffold project structure"
cat > .gitignore <<EOF # Python cache __pycache__/ *.py[cod]  # Virtual envs .venv/ env/ venv/ ai-alpha-engine-*/  # Jupyter .ipynb_checkpoints/  # OS .DS_Store EOF  git add .gitignore git commit -m "chore: add .gitignore"
ls -la .gitignore
pwd ls -la
cat > .gitignore <<EOF # Python cache __pycache__/ *.py[cod]  # Virtual envs .venv/ env/ venv/ ai-alpha-engine-*/  # Jupyter .ipynb_checkpoints/  # OS .DS_Store EOF
git add .gitignore git commit -m "chore: add .gitignore"
cat > .gitignore <<EOF
__pycache__/
*.py[cod]
.venv/
env/
venv/
ai-alpha-engine-*/
.ipynb_checkpoints/
.DS_Store
